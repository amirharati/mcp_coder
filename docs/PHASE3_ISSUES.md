<!--
  STEWARDSHIP — Tier 2 Phase 3 issue tracker. See docs/VISION_DOCS.md.

  - Planning session adds/fixes rows from worker § Results or E2E findings.
  - Workers: note issues in local spec § Results; do not edit this file.
  - Carried from Phase 2: see P2-ISS-* disposition in frozen PHASE2_ISSUES.md.
-->

# Phase 3 issue tracker

**Purpose:** Gaps, limitations, and follow-ups discovered during Phase 3 work (and carried from Phase 2 exit).
**Milestone board:** [PHASE3_MVP.md](./PHASE3_MVP.md)
**Phase 2 issues (frozen):** [PHASE2_ISSUES.md](./PHASE2_ISSUES.md)
**Backlog:** [BACKLOG.md](./BACKLOG.md)

Status: `open` | `scheduled` | `done` | `wontfix-p3` | `carried`

| When to fix | Milestone |
|-------------|-----------|
| Within Phase 3 | Nearest P3-* task in [PHASE3_MVP.md](./PHASE3_MVP.md) |
| Phase 4+ | `wontfix-p3` + link BL-* |

---

## Issues

| ID | Status | Priority | Title | Target | Notes |
|----|--------|----------|-------|--------|-------|
| P3-ISS-001 | `done` | medium | `files_changed` misses new paths without git | P3-322a / BL-322a | Fixed 2026-06-08 — manifest tracker-primary |
| P3-ISS-002 | `carried` | medium | Failed-delegate audit trail weak vs JSONL | P3-320 / BL-320 | From **P2-ISS-007**; report Blockers overwrite |
| P3-ISS-003 | `scheduled` | medium | Planner can ignore `contract_warnings` | P3-311 / BL-311b | From **P2-ISS-004**; auto-merge read paths when `spec_path` set |
| P3-ISS-004 | `scheduled` | low | Tiered executor model selection | P3-321 / BL-321 | From **P2-ISS-008** |

---

## P3-ISS-001: `files_changed` misses new paths without git

**Carried from:** P2-ISS-002 (frozen `wontfix-p2` at P2-499).

**Problem:** Without git, audit falls back to mtime on `target_files` only — new files outside hint are invisible.

**Target:** P3-322a — workspace manifest walk + delta in `workspace_history.db`; wire into `files_changed` / `files_unexpected`.

**Acceptance:** Non-git E2E delegate lists all created/modified paths under workspace (P2-ISS-002 dogfood scenario). ✓ P3-322a unit tests + 355 pytest.

---

## P3-ISS-002: Failed-delegate audit trail weak vs JSONL

**Carried from:** P2-ISS-007.

**Problem:** Main `specs/reports/*.md` overwrites Blockers on success; Run log truncated; JSONL canonical but not planner-visible.

**Target:** BL-320 / P3-320 — attempt archive under `.mcp-coder/specs/attempts/`; optional `list_delegation_attempts` MCP tool.

**Acceptance:** N failed implements → N attempt files (when config on); report links last N attempts.

---

## P3-ISS-003: Planner can ignore `contract_warnings`

**Carried from:** P2-ISS-004.

**Problem:** Warn-only read-dep gaps; delegation runs; broken layout possible if planner ignores warnings.

**Mitigation today:** Cursor rules v7; compiler materializes spec Read paths when context package on — but `target_files` hint gaps still warn.

**Target:** BL-311b — auto-merge spec Files read paths into delegate context when `spec_path` set (optional config); stronger MCP summary for planner.

**Acceptance:** Read-dep omit dogfood either pre-blocked or auto-included; no stray nested tree on success path.

---

## P3-ISS-004: Progressive / tiered executor model selection

**Carried from:** P2-ISS-008.

**Problem:** Global `AIDER_MODEL`; manual `.env` swap on failure.

**Target:** BL-321 / P3-321 — tier catalog, optional `model_tier` on delegate, config-gated auto step-up.

**Acceptance:** Documented tiers; JSONL records tier resolution; optional one step-up on `timeout`.

---

## How to add an issue

1. Append row to table with next `P3-ISS-NNN`.
2. Add `### P3-ISS-NNN` section: Found, Problem, Impact, Target, Acceptance.
3. Long-term only → [BACKLOG.md](./BACKLOG.md).
4. Workers reference ID in spec § Results only.

---

## Changelog

| Date | Change |
|------|--------|
| 2026-06-08 | Tracker created at Phase 3 start; P3-ISS-001–004 carried from P2 exit |
| 2026-06-08 | **P3-ISS-001 done** — P3-322a manifest attribution |
