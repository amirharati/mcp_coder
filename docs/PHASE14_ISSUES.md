<!--
  STEWARDSHIP — Phase 14 issues log. See docs/VISION_DOCS.md.

  - OK: log bugs found during implementation or audit, add workaround notes, mark fixed.
  - NOT OK: replan milestones or change locked decisions here; do that in PHASE14_MVP.md.
  - Workers: file issues here when found; fix in task spec § Results.
-->

# Phase 14 issues

**Status:** **Active** — Phase 14 opened 2026-06-23.
**Open:** none (all 11 closed as of 2026-06-25)
**Closed:** P14-ISS-001 through P14-ISS-011
**Related PM board:** [PHASE14_MVP.md](./PHASE14_MVP.md)

---

## Closed implementation issues

| ID | Status | Severity | Summary | Milestone | BL | Resolution |
|----|--------|----------|---------|-----------|-----|------------|
| P14-ISS-001 | closed | structural | Executor `llm_call` summary event omits `reasoning_tokens` (different code path from helper) | P14-003 / P14-ISS-FIX | BL-534 / BL-557 | **Fixed 2026-06-25 (master, inline).** Two-layer fix: (1) `build_executor_llm_trace_record()` (P14-ISS-FIX, earlier) now copies `reasoning_tokens`/`cached_tokens` + top-level `thinking_tokens` alias when present in the `tokens` dict — parity with helper path. (2) `aider_engine.py::_execute_delegation()` (today, BL-557 overlay) now overlays `reasoning_tokens`/`cached_tokens` from the litellm callback accumulator (`get_accumulated_usage(delegation_id, ROLE_EXECUTOR)`) after `_extract_tokens()` returns, since Aider's Coder attrs don't surface reasoning counts even though the callback captured them. Verified in live trace: executor `llm_call` now carries `tokens.reasoning_tokens=490` + `thinking_tokens=490` matching `backend_llm_call`. 3 new tests in `tests/test_p14_iss_001_executor_overlay.py`. |
| P14-ISS-002 | closed | structural | Helper `llm_call` events silently omit `reasoning_tokens` when provider returns none (no `reasoning_unavailable` reason emitted) | P14-003 / P14-ISS-FIX | BL-534 | **Fixed 2026-06-25 (P14-ISS-FIX, master re-implementation).** `build_trace_record()` now always emits `reasoning_tokens` key (null when model returned none) so consumers can distinguish "field absent = capture broken" from "field null = model chose not to think". Verified in live dogfood traces: helper `llm_call` records carry `reasoning_tokens=0, cached_tokens=0, thinking_tokens=0` for non-thinking turns. Test: `tests/test_p14_iss_002_helper_reasoning_null.py`. |
| P14-ISS-003 | closed | footgun | `AIDER_MODEL` silently overrides `MCP_CODER_MODEL` for executor | P14-003 / P14-ISS-FIX | BL-536 | **Fixed 2026-06-25 (master, inline).** `resolve_model_name()` in `core/config/models.py` now emits a `server_log_warn` when both `AIDER_MODEL` and `MCP_CODER_MODEL` are set and differ. Non-breaking; existing precedence preserved. |
| P14-ISS-004 | closed | — | `drop_params=True` vs OpenRouter passthrough — resolved (no gap) | P14-003 | — | **Closed 2026-06-24.** Dynamic smoke confirmed `extra_body={"reasoning":{"effort":"high"}}` + litellm-native `reasoning_effort='high'` both survive `drop_params=True`. No passthrough gap. Filed-and-closed by P14-003 worker. |
| P14-ISS-005 | closed | actionable | Executor path silently ignores `temperature`/`top_p`/`max_tokens` from `CallParams` | P14-003 / P14-ISS-FIX | BL-536 | **Fixed 2026-06-25 (P14-ISS-FIX, master re-implementation).** `_apply_executor_model_params()` in `aider_engine.py` now routes `temperature`/`top_p`/`max_tokens` into `model.extra_params` as top-level litellm kwargs (only when user hasn't already supplied via `EXTRA_PARAMS` escape hatch). `model_registry.py::policy_applied()` removed the stale `ignored` note. Test: `tests/test_p14_iss_005_executor_params.py` + `test_model_registry_p9_011.py`. Doc note added to `docs/guide/env-vars.md`. |
| P14-ISS-006 | closed | doc | Inconsistent reasoning param shape between helper and Aider paths | P14-003 / P14-ISS-FIX | BL-534 | **Fixed 2026-06-25 (master, inline).** Doc note added to `docs/guide/env-vars.md` clarifying helper (top-level `reasoning_effort`) vs Aider (`extra_body={"reasoning":{"effort":"high"}}` for OpenRouter-native) param shapes. Both work dynamically; Aider's design, not our code. |
| P14-ISS-007 | closed | robustness | Helper path has no explicit `timeout` or `num_retries` | P14-003 / P14-ISS-FIX | BL-534 | **Fixed 2026-06-25 (master, inline).** `LlmGateway.complete()` in `core/observability/gateway.py` now wires `MCP_CODER_HELPER_TIMEOUT` (default ~300s) and `MCP_CODER_HELPER_NUM_RETRIES` (default ~2) via new `_resolve_int_env` helper. Documented in `docs/guide/env-vars.md` new "Helper LLM control" section. |
| P14-ISS-008 | closed | structural | `_emit_llm_call_event` imported nonexistent `build_llm_call_record` (should be `build_trace_record`) — supervisor `llm_call(role=supervisor)` trace events were silently never emitted | P14-001 | BL-534 / BL-536 | **Fixed 2026-06-24 (P14-001 worker, inline).** Two-line fix: import + call renamed to `build_trace_record`. Verified supervisor `llm_call` events now emit. Cross-linked BL-534/536. This was the canonical "best-effort writes that silently swallow errors" pattern that motivated P14-ISS-010 (swallow-counter) in the P14-004 cleanliness audit. |
| P14-ISS-009 | closed | structural | Helper roles missing `backend_llm_call` companion event (asymmetric capture vs executor) | P14-004 | BL-534 | **Fixed 2026-06-25 (P14-ISS-FIX, master re-implementation + automated dogfood correction).** `core/observability/litellm_callback.py::record_owned_completion()` now calls new `_emit_helper_companion_events()` which emits `backend_llm_call` with `call_type="owned_helper"` + `(role, call_index)` so the viewer can join on the same key as the executor triple. Initial implementation also emitted a `proxy_llm_call` with `attribution_source="gateway"`, but automated dogfooding revealed helpers *already* receive a `proxy_llm_call` with `attribution_source="headers"` from the local proxy — the second one duplicated. Removed the redundant `proxy_llm_call`; helpers now get `llm_call` + `backend_llm_call` (proxy comes from the local proxy HTTP hop). Verified in live dogfood: 34/34 helper calls across 6 delegations have the companion `backend_llm_call`. Test: `tests/test_p14_iss_009_helper_event_triple.py`. |
| P14-ISS-010 | closed | robustness | Silent-swallow `except Exception: pass` sites hide errors (pattern from P14-ISS-008) | P14-004 | BL-558 | **Fixed 2026-06-25 (P14-ISS-FIX, master re-implementation).** Supervisor swallow sites in `core/engine/supervisor.py` (`_emit_llm_call_event`) and `core/engine/supervisor_agent.py` (`_llm_decide` fallback, reviewer call fallback) now log a `WARNING` with `exc_info=True` and bump a module-level counter via new `core/engine/_swallow_counts.py::bump_supervisor_swallow_count()`. Counter is exposed for future health endpoint (BL-558). Test: `tests/test_p14_iss_010_swallow_visibility.py`. |
| P14-ISS-011 | closed | cleanliness | Redundant `supervisor_turn_decision` trace events (dedup needed) | P14-004 | — | **Fixed 2026-06-25 (P14-ISS-FIX, master re-implementation).** `core/engine/supervised_io.py::_record_decision()` now deduplicates `supervisor_turn_decision` emissions by a SHA-256 key of `(decision_name, reasoning[:200], risk_tier, question_stripped)`. Repeated decisions with the same key are suppressed and counted in `suppressed_duplicate_decisions`. Verified in live dogfood: 0 redundant `supervisor_turn_decision` events across 6 delegations. Test: `tests/test_p14_iss_011_decision_dedupe.py`. |

---

## Changelog

| Date | Event |
|------|-------|
| 2026-06-25 | **All 11 P14-ISS items now closed.** P14-ISS-FIX worker (GLM, single-go) reported completion of 6 remaining issues (001/002/005/009/010/011); master verification found 5 of 6 source implementations did not persist to disk (only test files + `_swallow_counts.py` survived). Master re-implemented all 5 missing fixes inline following the spec, then ran automated dogfood (`scripts/p14_004_dogfood_run.py`) which caught a redundant `proxy_llm_call` bug in the P14-ISS-009 implementation — corrected and re-verified. P14-ISS-001's executor `llm_call` overlay (BL-557) was a follow-up gap discovered during manual dogfood viewer analysis: `build_executor_llm_trace_record` would propagate `reasoning_tokens` if present, but `_extract_tokens()` in `aider_engine.py` never populated it from the callback accumulator — fixed with a 5-line overlay after `resolve_executor_tokens()`. 20+ new ISS tests pass; 3 pre-existing unrelated failures (FTS query, supervised_io_p11_002) unchanged. |
| 2026-06-24 | **P14-004 audit worker returned** — filed P14-ISS-009 (helper missing `backend_llm_call`), P14-ISS-010 (silent-swallow sites), P14-ISS-011 (redundant decision events). 7 missing viewer handlers fixed inline. New `scripts/p14_004_capture_audit.py` + `docs/notes/p14-004-viewer-parity-map.md` artifacts. |
| 2026-06-24 | **P14-002 worker returned** — no new P14-ISS filed. Plan was unambiguous and executed mechanically. Two slices shipped: autonomous `confirm_ask` interception (BL-547 v1) and reviewer findings injection (BL-543 C start). 17 new tests pass. One pre-existing unrelated failure noted by worker. |
| 2026-06-24 | **P14-001 worker returned** — filed + fixed P14-ISS-008 (`_emit_llm_call_event` imported nonexistent `build_llm_call_record`; supervisor trace events silently never emitted for months; two-line fix, verified). Cross-linked BL-534/536. Notable for P14-004c (silent-swallow pattern). |
| 2026-06-24 | **P14-003 worker returned** — filed P14-ISS-001..007. ISS-001 (executor trace omits reasoning_tokens, structural, BL-534); ISS-002 (helper silent omission, structural, BL-534); ISS-003 (`AIDER_MODEL` overrides `MCP_CODER_MODEL` footgun, BL-536); ISS-004 (drop_params passthrough — closed, no gap); ISS-005 (executor ignores temperature/top_p/max_tokens, actionable, BL-536); ISS-006 (inconsistent reasoning param shape, doc-only, BL-534); ISS-007 (helper path no timeout/retries, robustness, BL-534). All cross-linked to BL-534/536 at filing time per protocol. |
| 2026-06-23 | Phase 14 issues log opened. |
